// import userService from "../services/userService";

describe("user tests ", () => {
  test("easy test", () => {
    // let isValid = userService.getUserInfoFromBody({});
    // expect(isValid).toBe(null);
  });
});
